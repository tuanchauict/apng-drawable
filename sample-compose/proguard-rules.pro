# Add project specific ProGuard rules here.
# The apng-drawable library ships its own consumer rules (DecodeResult fields are
# accessed from native by name and must not be obfuscated), so nothing extra is
# required here for the sample.
